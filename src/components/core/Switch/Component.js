import React from 'react';
import { Switch as RNComponent } from 'react-native';

const Switch = (props) => <RNComponent style={{ transform: [{ scaleX: .8 }, { scaleY: .8 }] }} {...props} />;

export default Switch;
