import React from 'react';
import { View } from 'react-native';
import { styled } from './Pools.styled';

const Pools = (props) => {
  return <View style={styled.container} />;
};

Pools.propTypes = {};

export default React.memo(Pools);
