import React, {memo} from 'react';
import {View} from 'react-native';
// import PropTypes from 'prop-types';

const RemovePool = () => {
  return (
    <View />
  );
};

RemovePool.propTypes = {};

export default memo(RemovePool);
